# Object allocation + property access churn
# perf: min_speedup 0.25   # recalibrated B7-c: vm (not interp) is the reference lane
mut i = 0
mut total = 0
while i < 100000 {
  let o = {x: i, y: i + 1, z: i + 2}
  total = total + o.x + o.y + o.z
  i = i + 1
}
inspect(total)
