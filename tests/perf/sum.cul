# Tight iteration loop (integer arithmetic)
# perf: min_speedup 2.5   # recalibrated B7-c: vm (not interp) is the reference lane
mut i = 0
mut acc = 0
while i < 10000000 {
  acc = acc + i
  i = i + 1
}
inspect(acc)
