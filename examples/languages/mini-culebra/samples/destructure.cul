# destructuring
let (p, q) = (1, 2)
println(p + q)
let [r, s2] = [30, 40]
println(r + s2)
let (u, v) = [5, 6]
println(u * v)
try { let [x1, x2, x3] = [1, 2]
      println(x1) } catch e { println(e.kind) }
