# Mutual recursion between named fns (sibling forward reference).
fn is_even(n) {
  if n == 0 {
    1
  } else {
    is_odd(n - 1)
  }
}
fn is_odd(n) {
  if n == 0 {
    0
  } else {
    is_even(n - 1)
  }
}
println(is_even(10))
println(is_odd(7))
println(is_even(3))
