// scalars and their operators
show('int', 1 + 2 * 3);
show('float', 0.1 + 0.2);
show('div', 7 / 2);
show('mod', 7 % 3);
show('neg mod', -7 % 3);
show('pow', 2 ** 10);
show('big', 1e21);
show('small', 0.000001);
show('smaller', 1e-7);
show('concat', 'a' + 1 + 2);
show('num first', 1 + 2 + 'a');
show('minus str', '10' - 3);
show('bool math', true + 1);
show('nan', 0 / 0);
show('inf', 1 / 0);
show('neg zero', -0);
show('cmp', [1 < 2, '10' < '9', 10 < 9, 'a' < 'b', null == undefined, null === undefined]);
show('eq', [1 == '1', 1 === '1', NaN === NaN, 0 == false, '' == false, [1] == 1]);
show('typeof', [typeof 1, typeof 'x', typeof true, typeof undefined, typeof null, typeof {}, typeof [], typeof fmt]);
show('truthy', [!!0, !!'', !!'0', !![], !!{}, !!NaN, !!null]);
show('logic', [1 && 2, 0 || 'x', null ?? 'd', 0 ?? 1, '' || 'y']);
show('bits', [5 & 3, 5 | 3, 5 ^ 3, ~5, 1 << 4, -16 >> 2, -16 >>> 28, 2 ** 32 | 0]);
show('ternary', 1 > 2 ? 'yes' : 'no');
show('string index', 'hello'[1] + 'hello'.length);
show('template', `x=${1 + 1} and ${'s'}`);
show('inc', (function () { let i = 5; let a = i++; let b = ++i; return [a, b, i]; })());
show('compound', (function () { let s = 'a'; s += 'b'; let n = 10; n -= 3; n *= 2; return [s, n]; })());
show('parse', [Number('12.5'), Number(''), Number('x'), parseInt('42px'), parseFloat('3.14abc'), parseInt('ff', 16)]);
show('to string', [String(1.5), String(100), (255).toString(16), (3.14159).toFixed(2), String(true)]);
show('math', [Math.floor(-1.5), Math.ceil(1.2), Math.round(2.5), Math.abs(-3), Math.max(1, 5, 3), Math.min(), Math.sqrt(16)]);
show('int64ish', [2 ** 53, 2 ** 53 + 1, 9007199254740993]);
show('exp form', [1.5e300, 123456789012345680000, 1e-10]);
