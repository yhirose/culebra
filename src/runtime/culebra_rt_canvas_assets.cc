// Strong bodies of image::decode_png and _canvas_detail::ttf_* — the
// Canvas-assets AOT axis (image.h). Built with the core archive's
// CULEBRA_CANVAS_WINDOW setting (CMakeLists) so screen_scale() stays a link.
#define CULEBRA_RT_CANVAS_ASSETS_STRONG
#include <image.h>
#include <font_ttf.h>
